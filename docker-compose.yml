services:
  web:
    container_name: campus-map-frontend

    build:
      context: .
      dockerfile: Dockerfile
      args:
        NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE}

    image: campus-map-kz-frontend:latest

    restart: unless-stopped

    ports:
      - "3005:80"