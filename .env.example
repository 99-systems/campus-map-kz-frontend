# Copy to .env for local overrides. .env is gitignored — do not commit secrets.
# Only NUXT_PUBLIC_* variables are exposed to the browser bundle.

NUXT_PUBLIC_API_BASE=

# Host port for docker compose (maps to container :80)
PORT=80
